env:
  state_dim: 8
  action_dim: 4
  workload_levels: [1, 2, 3, 4, 5]

training:
  total_steps: 300000
  batch_size: 128
  gamma: 0.98
  actor_lr: 0.0001
  critic_lr: 0.0001
  device: "cuda"  # or "cpu"

results:
  output_dir: "results"
